# 安全检查清单

- [x] 应用启动时拒绝默认弱 JWT_SECRET、示例数据库口令和默认对象存储密钥（生产）
- [x] 应用启动时拒绝 CORS `*`（生产）
- [ ] 登录接口限流生效
- [ ] 敏感删除要求二次确认（step-up）
- [ ] MFA 可对管理员强制启用（策略配置）
- [ ] 抽查：无权限返回 403；停用插件返回 404 / 菜单消失
- [ ] 抽查：DataScope SELF 不可越权读取他人资源
- [ ] 依赖漏洞扫描（`pnpm audit`）无未关闭高危

> 以上两项是源码启动门禁；仍需在真实预发/生产部署中用实际 Secret 和 CORS 值完成运行验收。

> 条件项（Redis / 完整多租户 / 热插拔）见 roadmap Phase 5 触发条件，未满足前不提前实现。
