import { roleDataScopeSchema, roleSchema, rolesPageSchema, userStatusSchema } from '@zen/shared'

import type { Role, RoleDataScope, RolesPage, UserStatus } from '@zen/shared'

export type { Role, RoleDataScope, RolesPage, UserStatus }
export { roleDataScopeSchema, roleSchema, rolesPageSchema, userStatusSchema }
