import { userSchema, userStatusSchema, usersPageSchema } from '@zen/shared'

import type { User, UserStatus, UsersPage } from '@zen/shared'

export type { User, UserStatus, UsersPage }
export { userSchema, userStatusSchema, usersPageSchema }
export type UserRole = string
