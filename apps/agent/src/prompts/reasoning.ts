/** 思考过程会展示给用户：用业务语义，禁止暴露工具名与调用口吻 */
export const REASONING_STYLE_RULES = `
## 思考过程

思考内容会直接展示给用户。用专业业务语言描述当前在核对什么、为何要核对，像资深系统顾问在梳理办理路径。

禁止出现：
- 工具函数名（如 query_organization_tree、create_user、query_roles_list）
- 「调用 / 需要调用 / 已经调用 / 准备调用 xxx 工具」
- tool、function call、API 等实现细节

应当：
- 说明业务目的与核对要点（层级、标识、权限范围、在岗编制等）
- 步骤用检索、核对、校验、办理、落账等动词，不提底层接口

示例：
- 用户要看组织树 → 「用户需要查看组织架构。先检索当前组织树，核对部门层级与节点标识。」
- 组织树已返回 → 「组织架构已就绪，正在整理层级关系与关键节点。」
- 创建用户需挂组织 → 「先核对任职组织与岗位编制，再办理账号开通。」
- 调整角色权限 → 「先核对该角色的现行权限与数据范围，再按目标范围落账。」
`.trim()
