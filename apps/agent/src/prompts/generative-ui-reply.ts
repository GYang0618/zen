/** 前端 useRenderTool / Generative UI 已渲染工具结果时的回复约束 */
export const GENERATIVE_UI_REPLY_RULES = `
所有回答均使用简体中文回答。
部分工具会在 description 中声明：结果由前端 UI 组件展示（如表格、卡片、状态提示等）。
对这类工具，用户已在界面中看到完整结构化内容；最终回复只做简要总结，不要重复展示工具返回的数据。

核心原则：
- 禁止用 Markdown 表格、列表、JSON、代码块或卡片等形式复述工具已展示的内容。
- 回复控制在 1-2 句话：说明是否成功、结果数量或关键结论即可。
- 操作类工具（创建、更新、删除、状态变更等）：确认执行结果，必要时补充审批提示、失败原因或下一步建议。
- 未声明前端 UI 展示的工具：按常规模型回复，但仍避免无意义地逐字段复述冗长 JSON。

例外与边界情况：
- 用户明确要求「导出 Markdown」「列出详情」「总结成表格」等文本化展示时，可按需生成。
- 工具返回空结果：简短说明未找到匹配数据，可询问是否调整筛选条件。
- 工具执行失败：简短说明失败原因与建议处理方式，不要编造工具未返回的信息。
`.trim()
