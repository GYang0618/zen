# Button migration

- Previous primitive: Radix `Slot`.
- Current primitive: internal `Slot` adapter backed by Base UI-compatible rendering.
- Preserved: variants, sizes, `asChild`, keyboard semantics, focus rings, and existing design tokens.
- Verification: `packages/ui` and Web TypeScript checks.
