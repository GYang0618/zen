{
  "name": "zen",
  "private": true,
  "scripts": {
    "web": "pnpm -F web dev",
    "api": "pnpm -F api dev",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "docs:check-links": "node scripts/check-docs-links.mjs",
    "check:permissions": "node scripts/check-permission-codes.mjs",
    "check:unguarded-writes": "node scripts/check-unguarded-writes.mjs",
    "check:plugin-guards": "node scripts/check-plugin-guards.mjs",
    "check:default-agent": "node scripts/check-default-agent-boundaries.mjs",
    "check:agent-schema": "node scripts/validate-agent-runtime-schema.mjs",
    "check:rbac": "pnpm run check:permissions && pnpm run check:unguarded-writes && pnpm run check:plugin-guards && pnpm run check:agent-schema",
    "plugin:validate": "pnpm -F @zen/plugin-sdk build && pnpm -F @zen/plugin-sdk validate",
    "plugin:generate": "pnpm -F @zen/plugin-sdk build && pnpm -F @zen/plugin-sdk generate && pnpm -F @zen/plugin-sdk build",
    "plugin:check": "pnpm -F @zen/plugin-sdk build && pnpm -F @zen/plugin-sdk generate -- --check",
    "plugin:create": "node scripts/create-plugin.mjs",
    "clean": "pnpm run clean:dist && pnpm run clean:modules",
    "clean:dist": "pnpm -r --parallel exec rimraf -rf dist",
    "clean:modules": "pnpm -r --parallel exec rimraf -rf node_modules && rimraf -rf node_modules",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@biomejs/cli-darwin-arm64": "^2.5.5",
    "@types/node": "^26.1.1",
    "@typescript/native": "npm:typescript@7.0.2",
    "husky": "^9.1.7",
    "lint-staged": "^17.2.0",
    "rimraf": "^6.1.3",
    "turbo": "^2.10.7",
    "typescript": "npm:@typescript/typescript6@6.0.2"
  },
  "engines": {
    "node": ">=24.14.1"
  },
  "packageManager": "pnpm@11.7.0",
  "volta": {
    "node": "24.14.1",
    "pnpm": "11.7.0"
  }
}
